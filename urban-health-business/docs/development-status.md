# Urban Health Business 开发状态与剩余缺口

> 更新日期：2026-08-09
> 分支：`urban-health-business`  
> 本文是开发总纲的执行状态附件，不取代各模块开发大纲。

最新完成条件审计与明确停工点见：

- `docs/testing/gis-completion-evidence-matrix.md`；
- `docs/development-log/2026-07-31-gis-stop-point.md`。
- `docs/development-log/2026-08-09-main-parity-np01-np03-handoff.md`；NP-04完成记录见
  `docs/development-log/2026-08-09-np04-standard-binding.md`；NP-05/06完成记录见
  `docs/development-log/2026-08-09-np05-np06-completion.md`。

## 2026-08-09 功能对齐阶段检查点

本轮依据 `docs/main-parity-next-development-outline.md` 开发，已完成 NP-01—NP-06。

已完成：

- NP-01 住宅小区识别与台账治理：独立待确认快照、确认幂等、重复识别、边界过期、合并、拆分、恢复和引用安全阻断；
- NP-02 外业任务闭环：问题类型、户数、位置、照片进度、既有 UploadSession 关联、完成校验和失败重试；
- NP-03 AI 配置与用户 Key：身份隔离、AES-256-GCM 本地加密、模型偏好、健康检查、脱敏元数据和用户范围模型调用；
- NP-04 问题编码与整改建议关联：正式问题可选绑定问题类型，指标由标准库关系派生，整改建议按同一标准库版本冻结，绑定/解除/不适用均有修订审计，未绑定问题仍合法；
- NP-05 CloudBase真实运行接线：正式 `cloudbase` Provider 组装、业务Collection契约、Repository/Storage适配、运行健康检查、迁移计划/执行/回滚和未验收安全护栏；
- NP-06 成果中心与系统设置：跨项目有界汇总、问题/报告索引、项目权限过滤、Provider/AI/高德/标准库设置状态和 Business 全局入口；
- Business 页面已增加上述三项的操作入口，未引用 Demo 固定结果；
- `main` 原版入口和 V9.1 Demo 未修改。

本检查点验证：

- `npm run check` 通过；
- `npm test` 通过，215/215；
- `npm run test:integration` 通过，1/1；
- `npm run test:e2e` 通过，11/11；
- `npm run verify:demo` 通过，42 个快照文件一致；
- `npm run verify:boundary` 通过；
- `git diff --check` 通过。

本轮已完成 NP-05、NP-06，并补齐迁移请求幂等、重复执行/回滚保护、本地 JSON 备份验证和无项目范围权限隔离。CloudBase 生产 `productionVerified` 仍为 false，需在真实隔离环境完成凭据与恢复演练后再变更。

## 1. 当前可运行结果

本地运行拓扑：

```text
浏览器 http://127.0.0.1:4182/business/
    ↓
Urban Health Business BFF :4182
    ├─ Business扩展数据 .data/
    └─ smart-renew本地后端 :4173
```

Demo入口：

```text
http://127.0.0.1:4182/demo/
```

Business首页直接读取4173当前项目列表；空库时显示真实空状态并允许从“新建项目”开始，不会加载Demo数据。

## 2. 已完成

### 2.1 项目隔离与基础设施

- 新版全部位于 `urban-health-business/`；
- 原smart-renew根目录未修改；
- V9.1 Demo复制为只读快照；
- Demo完整性校验已建立；
- 原项目边界校验已建立；
- 同一BFF进程内按项目串行写协调；
- Business入口、BFF和上游代理已建立；
- 六阶段统一工作流聚合已建立；
- requestId结构化请求完成日志和进程运行指标；
- 能力声明明确区分本地JSON数据仓储、托管数据库、本地文件存储和对象存储；
- AB-00 LegacyCapabilityRegistry和外业、ProjectData、Legacy迁移、旧报告只读适配器；
- 项目、照片、分析、Candidate、正式问题、报告等对象的唯一主数据源注册表；
- Business主数据优先、旧数据只读兼容的统一去重读取策略；
- Business服务对原照片、分析、问题和报告的调用统一收敛到smart-renew适配层；
- 空数据、失败、阻塞和unavailable状态不使用Demo回退。

### 2.2 项目与阶段01

- 真实项目创建；
- 项目名称、区域、类型、范围和说明修订；
- 项目档案修订冲突检测；
- 项目级跨仓储JSON导出；
- ProjectData过滤、搜索、记录数组导入和JSON Envelope导出BFF；
- 外业小区/楼栋查询、任务创建、单项读取及刷新后列表恢复；
- 外业任务主体保留在smart-renew，Business仅保存任务引用；
- Legacy迁移预检、显式确认、运行审计、失败留痕和请求幂等；
- 导出清单、Schema版本和照片二进制包含声明；
- 人工小区层级建立；
- 小区编辑、软停用和恢复；
- 小区修订冲突检测；
- 人工楼栋台账；
- 楼栋户数、单元数和层数；
- 楼栋编辑、软停用和恢复；
- 楼栋修订冲突检测；
- 项目边界经纬度录入；
- 边界点、自相交、面积和项目修订校验；
- 项目边界面积与中心点计算；
- 项目边界修订快照和历史界面；
- 高德浏览器地图运行配置、缺配置和加载失败状态；
- 地址正向解析经BFF调用高德Web服务，服务端Key不下发；
- 高德GCJ-02项目边界绘制、已有边界回显和后端二次校验；
- PDF、JSON/GeoJSON、CSV、TXT、XLSX、DOCX和ZIP资料资产两步上传；
- 资料资产本地二进制持久化、SHA-256、幂等登记、下载、软停用和恢复；
- 资料资产大小/MIME失败留痕及刷新后的同资产续传；
- 已完成资料二进制不可变、同哈希幂等和项目内重复引用治理；
- 资料资产修订冲突检测和独立治理人员记录；
- 单一无孔洞GeoJSON Polygon导入项目边界；
- CSV、JSON、GeoJSON只读结构预览及前端字段/样例展示；
- 边界版本保存来源资料ID和内容哈希；
- 项目导出包含资料资产元数据并明确排除其二进制；
- 真实照片选择、原始二进制上传和服务端文件持久化；
- 照片绑定真实小区和可选楼栋；
- 照片显示名称、小区/楼栋、拍摄时间、经纬度、坐标系和备注治理；
- 照片治理修订号、冲突检测、软停用和恢复；
- 1—200张CSV坐标清单批量治理及逐项成功/失败结果；
- 停用照片从业务统计、AI任务和人工问题证据中剔除；
- 照片治理覆盖层纳入项目JSON导出，且不改写原照片二进制；
- 项目照片列表和缩略图；
- 上传前格式、大小和小区绑定校验；
- 上传会话持久化、幂等创建、失败保留和同会话重试；
- 上传队列刷新恢复、取消、字节数、哈希和尝试次数记录；
- 资料完整度6项必需规则和4项建议规则；
- 实时完整度结果、阶段01状态联动和人工校验快照归档；
- 完整度校验历史纳入项目JSON导出；
- 空项目可从界面启动业务。

### 2.3 阶段02

- 真实照片选择；
- AI能力状态读取；
- 持久化异步AI任务；
- queued/running/completed/failed/canceled状态；
- 前端任务轮询和刷新恢复；
- 排队任务取消和失败任务重试；
- 服务重启后的未结束任务恢复；
- running/reviewing/failed分析记录持久化；
- 模型JSON校验；
- 候选动态标准化；
- AnalysisCandidate独立存储和按任务查询；
- AI任务保存照片内容哈希、治理修订、空间归属和坐标快照；
- 照片停用、治理修订或内容变化后的AI任务stale传播；
- stale候选在重新分析前阻止进入未归档人工复核；
- 风险分布和平均置信度动态计算；
- AI未配置时明确不可用；
- 历史真实分析在AI暂不可用时仍保持completed。

### 2.4 阶段03

- 候选逐项接受或排除；
- 候选标题、描述、证据和风险等级在线修正；
- 候选逐条保存、刷新恢复和旧分析候选自动建档；
- 候选乐观修订冲突检测与逐次审计轨迹；
- 候选最终归档审计和归档后只读；
- 修正字段白名单与modified状态；
- 复核人员记录；
- AI不可用时人工补录正式问题；
- 人工复核结论独立持久化；
- 0问题人工结论显式确认；
- 正式问题独立持久化；
- 正式问题修订冲突检测和问题级审计轨迹；
- 正式问题解除旧指标编码强依赖；
- 0正式问题合法归档；
- 只有人工接受/修正的候选进入正式问题。

### 2.5 阶段04

- 正式问题真实列表；
- 0问题合法空状态；
- 人工经纬度绑定；
- 经度、纬度范围校验；
- 问题点必须位于真实项目边界内，且坐标系必须一致；
- 坐标系、来源、确认人员和修订次数记录；
- 点位乐观修订冲突检测及前后坐标审计；
- 点位修订历史随正式问题选择在GIS工作台展示；
- 按真实经纬度绘制项目边界和问题点矢量预览；
- 选择正式问题后点击矢量预览回填真实经纬度；
- 50—10000米用户自定义半径问题分析；
- 空间分析参数、项目修订、问题修订、距离和命中结果快照；
- 高德底图项目边界和正式问题回显、点击底图回填GCJ-02问题坐标；
- 高德POI服务端分页查询和社区/街区分类规则；
- POI允许、硬排除、精确去重、近邻合并和住宅项目边界裁剪；
- POI原始Provider记录、查询参数、拒绝原因、清洗规则和结果快照；
- WGS84/GCJ-02混用硬阻断，不在未转换数据上生成误导结果；
- 边界/正式问题变化后的空间分析stale传播；
- 不使用西安静态图、百分比坐标或固定问题点。

### 2.6 阶段05

- 能力查询接口；
- draft输入输出契约；
- 未接入运行入口；
- 未接入时返回501；
- 原标准库412条记录的只读加载与汇总；
- 61个指标、35个问题分类、124个问题类型和124条整改建议查询；
- 指标工作台真实目录与数据来源说明；
- 界面不展示10个演示指标；
- 不生成权重、扣分或综合得分。

### 2.7 阶段06

- Business报告JSON快照；
- 报告版本号；
- 报告修订号和乐观冲突检测；
- 执行摘要、建议和内部备注编辑；
- 报告修订审计；
- 项目、正式问题、定位、分析批次快照；
- 报告保存照片集合和照片治理修订快照；
- 0正式问题报告；
- 指标未接入状态进入报告；
- 报告历史列表；
- 同项目任意两个报告版本的结构化比较；
- 标题、编辑内容、统计口径、问题、空间分析和照片证据差异；
- JSON文件下载；
- 按冻结快照动态生成独立打印版与浏览器另存PDF；
- 项目概况、风险摘要、问题、小区、空间研判、编辑结论、行动建议、标注照片画廊、指标缺失和来源索引章节；
- 报告原始照片证据与标注派生照片分离，标注图只通过正式问题引用进入画廊；
- 项目、正式问题、空间分析和照片证据变化后的报告stale传播；
- 生成新版本后允许最新报告恢复有效，历史过期版本仍保留原因；
- 不复用旧报告“必须有问题”和旧指标映射限制。

## 3. 已验证

- Node语法检查通过；
- 单元测试131项通过；
- 双服务隔离全过程集成测试1项通过；
- Demo快照42个文件完整性通过；
- 原项目修改边界校验通过；
- 浏览器空库首页和新建项目表单通过；
- 浏览器资料工作台显示“同步数据索引”和“导出SQLite”，索引按钮真实调用BFF并返回同步结果，控制台无错误；
- 隔离端到端链路通过：

```text
创建项目
→ 建立小区
→ 上传照片
→ 修订照片治理信息
→ 停用照片并验证业务统计和AI拦截
→ 恢复照片
→ 上传GeoJSON资料并完成哈希校验
→ 停用、恢复资料并导入为新边界版本
→ 上传SQLite资料并完成来源哈希校验
→ 转换ProjectData、重定向引用并重建原索引
→ 导出SQLite并完成往返解析
→ 校验资料完整度并归档快照
→ 保存分析候选
→ 人工接受
→ 形成无指标映射的正式问题
→ 保存真实坐标
→ 生成报告V1
→ 指标运行返回501
```

最终隔离工作流：

```text
01 completed
02 completed
03 completed
04 completed
05 unavailable
06 completed
```

## 4. 当前部分完成

本节只描述当前Business实际接入状态。原smart-renew已经实现但尚未接入的能力，按A/B复用工作包登记，不再误写成需要全部从零开发。已经完成的Business实现是接入基线，不属于本次待重写内容。

### 4.1 阶段01资料治理

已可运行，但存储适配仍是过渡实现。

A/B复用接入已完成：

- 原CloudBase对象存储Provider契约与Mock适配；
- 外业任务工作台界面；
- 现有WebP上传和照片归属校验与原核心的行为对照及单一调用路径收敛。

真实CloudBase环境尚未提供环境ID、权限和凭据，因此当前仍默认使用本地JSON与文件存储；`/api/meta.providers.cloudbase.productionVerified`如实为`false`。

C/D后续项：

- WebP EXIF、照片时区人工校正和批量归属历史；
- 对象存储直传、分片和字节级断点续传；
- 跨项目重复资料策略和重复引用合并；
- DOCX、XLSX、ZIP和无人机资料深度解析；
- 移动采集端；
- 完整度规则配置和项目类型模板。

### 4.2 阶段02 AI

当前已有进程内异步执行器和持久化任务。

A/B复用接入已完成：

- 超过20张时按每20张自动拆批和跨批合并；
- 原BBox IoU与标题归一化候选去重；
- 现有候选分类/BBox规范化与原算法的行为对照；
- 每批状态、照片证据快照、模型、requestId、usage和promptVersion元数据；
- 多批成功后形成单一可复核聚合分析，子批次标记为merged；
- 任一批次失败时阻止部分候选进入人工复核；
- 服务重启后复用已完成批次，只继续未完成批次。

C/D后续项：

- 独立后台任务进程和多实例抢占/租约；
- 运行中模型请求取消和单照片部分失败重试；
- AnalysisCandidate服务端分页和完整版本查询；
- 原始模型响应独立归档；
- 正式提示词、模型和分类版本管理；
- 更完整的跨项目stale传播。

### 4.3 阶段03人工复核

当前支持接受、排除、字段修正、人工补录、人工结论、候选修订和问题审计。

A/B复用接入已完成：

- 原风险筛选和批量接受交互；
- 原BBox百分比显示；
- 原Canvas标注图派生和照片归档步骤。

C/D后续项：

- 标注框拖拽编辑；
- 复核意见、附件和证据文件；
- 多人锁与协作修订；
- 批次级审计查询和失败恢复操作；
- Candidate服务端分页和通用批量操作。

### 4.4 阶段04 GIS

当前已完成V9.1 GIS专项本地交付，详细证据见第11节。

A/B复用接入已完成：

- 原高德地图SDK、地理编码、点击定位和边界绘制；
- 原POI分页检索；
- 原POI允许、排除、名称/地址归一化和空间去重；
- 原社区/街区分类规则；
- 原始POI、Provider、参数、清洗数量和规则版本持久化；
- 未配置、不匹配坐标系及Provider失败的真实状态。

仍属外部或非专项目标的能力：

- 小区、楼栋、地块和道路独立正式空间绑定；
- 政务权威GIS和法定测绘级空间算子；
- 真实高德账号预生产在线验收。

### 4.5 阶段06报告

当前已有JSON快照、内容编辑、修订审计、版本比较、下载和打印版。

A/B复用接入项：

- 原report-snapshot-core的来源快照适配及现有版本算法行为对照；
- 原动态项目报告章节；
- 原风险统计、表格和标注照片画廊。

C/D后续项：

- 正式ReportTemplate；
- 自由章节编排；
- 地图和图表快照服务；
- 服务端PDF和Artifact归档；
- 审批、签发、发布和分享；
- 指标结果接入后的正式章节。

### 4.6 跨阶段数据与迁移

已完成的A/B增量：

- 原ProjectData查询、过滤、搜索、记录数组导入和JSON Envelope导出BFF；
- JSON Envelope跨项目记录ID和引用重定向；
- SQLite SourceAsset扫描、已知表/交换表转换、导入、真实文件导出和引用重建；
- SQLite导入运行审计，以及SourceAsset ID、修订、内容哈希到ProjectData的来源追溯；
- 原外业层级和任务创建/读取BFF；
- 原Legacy迁移预检与显式执行适配；
- Business迁移运行记录、失败审计和请求幂等；
- 旧OfficialIssue到Business OfficialIssue的一次性迁移；
- 旧报告到Business只读版本迁移；
- 源指纹幂等判断和迁移后源数据变化冲突。

其余A/B增量已完成：

- 412条标准库、61指标、35问题分类、124问题类型和124条整改建议只读目录；
- CloudBase数据库与对象存储可选Provider、Collection配置、能力声明和Mock契约。

正式问题、Candidate、报告和仓储的唯一主数据源已由AB-00注册并进入统一读取策略；后续迁移只能显式执行，禁止形成可写双副本。

## 5. 完全待外部模块

### 5.1 指标引擎

负责人完成引擎后需要接入：

- 指标目录和版本；
- 输入快照Schema；
- 运行创建、查询和失败恢复；
- 指标结果Schema；
- 计算依据和数据质量；
- 问题与指标的外部规则；
- 权重和评分规则；
- 结果版本；
- 报告引用；
- stale传播。

Business当前不会自行恢复之前的10个演示指标、映射、扣分和权重。

## 6. 平台级缺口

本轮按用户要求未讨论P0安全，但运行可行性仍需要：

- 将原CloudBase数据库与对象存储代码封装为Business可选Provider；
- 为Business新增实体建立CloudBase Collection和契约测试；
- 数据迁移和Schema版本升级；
- 事务和跨仓储一致性；
- 正式数据库事务、原子乐观锁和多实例写协调；
- 全接口幂等键覆盖与重复提交审计；
- 独立后台任务执行器和多实例调度；
- 日志集中采集、长期指标存储和跨服务链路追踪；
- 配置中心；
- 备份恢复；
- 照片二进制归档包和项目导入恢复；
- 自动化契约测试和浏览器E2E；
- 生产构建与部署配置。

SQLite当前依赖Node.js 22.13以上的内置`node:sqlite`。本地Node.js 24会输出该模块的ExperimentalWarning，功能和往返测试均通过，但生产运行时仍应固定Node版本并完成升级回归，或在后续Provider阶段替换为经过选型的稳定SQLite驱动；不能忽略这一运行时约束。

原CloudBase数据库和对象存储代码现已封装为Business可选Provider并通过Mock契约验证，但尚未在真实云环境验证。它也没有事务、正式索引、多实例一致性或直传能力，不能把“Provider已实现”写成“生产数据库已完成”。

鉴权、权限、密钥和安全加固不在本轮范围，但上线前不能永久忽略。

## 7. 本次A/B复用开发范围

本次开发以`docs/reuse-first-ab-development-outline.md`为执行大纲，保留当前Business代码基线，只按技术依赖推进各工作包尚缺的A/B增量：

1. 已完成：固定主数据源、能力注册、专项适配器和统一读取策略；
2. 已完成：ProjectData记录查询/导入/导出、外业任务和原Legacy迁移BFF；
3. 已完成：SQLite转换、引用重建和来源追溯、Business正式问题/报告迁移；
4. 已完成：接入高德地图及POI，不重写现有边界和半径分析；
5. 已完成：补齐AI自动拆批、跨批合并和候选去重，保留现有任务与Candidate框架；
6. 已完成：人工复核筛选、批量接受、BBox显示和标注图派生；
7. 已完成：报告来源适配和动态Renderer，保留现有报告仓储；
8. 已完成：CloudBase可选Provider代码、能力声明和Mock契约；
9. 已完成：标准库和整改建议只读目录；
10. 当前收尾：全过程契约、集成、Demo和边界回归。

以上顺序只表示技术依赖，所有A/B工作包的“增量交付项”均属于本次范围。现有基线不重复开发；C/D能力继续以真实待接入状态保留。

## 8. 2026-07-26 开发停止点

今天开发到此停止，后续不得从旧大纲中的“高德地图待接入”状态重新开始。

### 8.1 当前代码基线

- 工作分支：`urban-health-business`；
- 当前功能提交：`eaf3c62 feat: integrate amap and poi analysis`；
- 前一功能提交：`692cfd2 feat: add sqlite exchange and business legacy migration`；
- 原smart-renew根目录没有因Business开发被修改；
- 本次没有执行远程推送。

### 8.2 今天已经完成

- ProjectData JSON Envelope与真实SQLite导入、导出、引用重建和来源审计；
- 旧OfficialIssue及旧报告向Business主仓储的显式、幂等迁移；
- 迁移报告只读保护和迁移后源数据变化冲突；
- 高德浏览器地图Controller与运行配置；
- BFF地址正向解析和服务端高德Web服务Provider；
- GCJ-02项目边界绘制、回显和后端二次校验；
- GIS工作台项目边界、正式问题回显和点击定位；
- POI分页查询、允许/硬排除、去重、近邻合并和住宅边界裁剪；
- 原始POI、Provider、参数、拒绝原因、清洗规则和结果快照；
- POI运行随项目边界变化进入stale，但不因正式问题集合变化误判stale；
- WGS84/GCJ-02混用阻断；
- 未配置高德时的真实不可用状态，不生成假边界或示例POI。

### 8.3 当前验证基线

- Node语法检查通过；
- 单元测试`108/108`通过；
- 双服务隔离全过程集成测试`1/1`通过；
- V9.1 Demo完整性`42`个文件通过；
- 原项目修改边界校验通过；
- 浏览器隔离验收通过：无高德配置时地图、地址和POI操作正确禁用，手工经纬度边界仍可保存；
- 浏览器验收临时服务已停止，隔离数据目录已删除；
- Git工作区在记录本停止点前为干净状态。

### 8.4 下次继续位置

下一增量从AB-05开始：

```text
超过20张真实照片
→ 每20张自动拆批
→ 保存批次及模型调用元数据
→ 跨批合并
→ 使用BBox IoU与标题归一化去重
→ 形成统一Candidate集合
→ 保持现有AnalysisJob和AnalysisCandidate为唯一业务主链
```

开始前先对照：

- `docs/reuse-first-ab-development-outline.md`中的AB-05；
- `docs/original-smart-renew-reuse-audit.md`中的AI拆批、BBox IoU和候选去重证据；
- 当前`AnalysisJobRunner`、`AnalysisCandidateRepository`和分析任务测试。

不得重写已有异步任务、候选仓储、stale传播和人工复核主链。

### 8.5 尚未完成的外部验收

真实高德在线验收需要部署环境提供：

```text
AMAP_JS_KEY
AMAP_JS_SECURITY_CODE
AMAP_WEB_SERVICE_KEY
```

当前已完成Mock契约、缺配置降级和浏览器流程验证，但尚未使用真实高德账号验证在线地图加载、地址返回质量、配额限制和真实POI结果。该事项属于部署配置后的验收，不应被误记为地图/POI代码尚未开发。

## 9. 2026-07-29 AB-05继续开发记录

### 9.1 已完成

- 服务端AnalysisJob支持任意已选照片按每20张自动拆批；
- 每批保存照片ID、照片证据快照、状态、上游分析ID、模型、requestId、usage和promptVersion；
- 多批结果合并为一个可进入人工复核的聚合分析；
- 同照片、同分类候选按BBox IoU大于0.42或标题归一化命中去重；
- 去重结果保留来源批次、来源分析、被合并候选ID和合并数量；
- 模型、requestId、usage和promptVersion聚合回AnalysisJob和聚合分析；
- 任一批次失败时整个Job失败，已完成批次标记为不可复核的部分结果；
- 服务重启恢复时读取已完成批次，只继续执行未完成批次；
- Business前端任务历史展示照片数、批次数和去重数量；
- BFF元能力声明增加analysisBatching和analysisCandidateDeduplication。

### 9.2 验证

- Node语法检查通过；
- 单元测试`115/115`通过；
- 双服务隔离集成测试`1/1`通过；
- V9.1 Demo完整性保持通过；
- 所有实现均位于`urban-health-business/`，原smart-renew业务源码未修改。

### 9.3 下次继续位置

下一增量从AB-06开始：

```text
人工复核风险筛选
→ 批量接受
→ BBox百分比显示
→ Canvas标注图派生
→ 标注图上传归档
→ 校准复核完成后的分析归档语义
```

继续开发时必须保留Candidate独立仓储、乐观修订、审计、ReviewSession、零问题结论和OfficialIssue主链。

## 10. 2026-07-29 AB-06继续开发记录

### 10.1 已完成

- 人工复核工作台支持全部/高/中/低风险筛选；
- “接受当前筛选中的待复核项”逐条调用Candidate PATCH，保留乐观修订和审计；
- BBox按0—999坐标转换为百分比覆盖层，并按原图比例显示；
- 抽取Canvas标注模块，按原图自然尺寸生成JPEG标注图；
- 标注图通过持久化上传会话归档，保存分析、原图、候选和操作人员来源；
- finalize在正式问题写入前校验标注上传完成状态，失败时保持分析可重试；
- 分析、候选和OfficialIssue保存标注照片及上传会话引用；
- 增加Business同源照片内容代理，供预览和Canvas读取；
- 标注派生照片与现场原图读模型隔离，不重复计入资料完成度、AI输入或报告原始证据；
- 候选保存、标注上传和最终归档均保留stale证据阻塞；
- 全部候选排除时仍可合法归档零问题，不生成无意义标注图。

### 10.2 验证

- Node语法检查通过；
- 单元测试`124/124`通过，覆盖BBox几何、Canvas绘制计划、派生上传来源、派生照片隔离、失败可恢复、stale和正式问题关联；
- 双服务隔离集成测试`1/1`通过，并覆盖Business同源照片内容代理；
- 本地Business页面加载无脚本错误，风险筛选控件和批量按钮已由浏览器验证；
- V9.1 Demo完整性保持`42`文件通过；
- 项目边界检查通过，原smart-renew业务源码未修改。

### 10.3 A/B工作包完成状态

AB-07动态报告快照与Renderer、AB-08 CloudBase可选Provider代码与Mock契约、AB-02外业工作台剩余界面，以及AB-10标准库和整改建议目录均已完成。现有报告仓储、版本算法、修订审计、比较、stale和零问题语义保持不变。

下一轮不再从A/B工作包重复开发。可继续的方向为真实CloudBase环境验证，或另行立项指标引擎、服务端PDF、对象存储直传、多实例一致性等C/D与外部模块。

### 10.4 最终回归

- Node语法检查通过；
- 单元测试`131/131`通过；
- 双服务隔离集成测试`1/1`通过，覆盖Provider元数据、标准库查询和动态报告打印；
- V9.1 Demo完整性`42`文件通过；
- 原项目业务源码边界检查通过；校验仅忽略macOS `.DS_Store`元数据，仍拦截Business目录外
  的源码和业务数据变更，该既有文件未被删除或改写。

## 11. 2026-07-30 V9.1 GIS完整能力开发记录

### 11.1 已完成的本地交付

- GIS-00～GIS-09全部实现：统一Geometry/CRS契约、地图聚合读模型、复杂边界、坐标转换、
  图层控制、问题/照片点位、参数分析、POI复核、踏勘路线、停留节点、照片关联和地图快照；
- GIS-10本地项完成：视口查询、Marker聚合、路线按缩放级别抽稀、10,000问题点与50,000点
  路线容量测试、GIS RBAC、审计身份覆盖、安全响应头、SQLite事务型GIS Provider、
  S3兼容地图快照对象存储、可校验备份脚本及生产容器/环境/恢复文档；
- 报告地图快照从冻结报告内容确定性生成SVG，保存哈希、源修订和存储引用，支持失败重试；
- 浏览器实测桌面端和375px移动端，修复移动端横向溢出；无高德Key时降级状态真实、无脚本错误；
- URL恢复项目、问题、空间运行、路线、图层选择和地图样式；同源浏览器保存当前用户显示
  偏好，刷新后全部正式状态仍从服务端重建；
- 地图工具补齐距离/面积测量及临时测量清理；问题点位支持1—200条批量校验确认；
- 路线起终点、清洗异常点、边界外空间对象告警及路线/照片修订stale传播已闭环；
- 报告地图快照包含参数分析范围、真实距离标签、图例和按配置显示的排除POI。

### 11.2 验证结果

- Node静态检查通过；
- 单元测试通过，包含10,000正式问题和50,000点路线容量用例；
- 双服务隔离集成测试通过，覆盖边界、问题、照片、路线、停留、照片关联和报告地图快照；
- 集成服务以`URBAN_HEALTH_AUTH_MODE=required`运行，实际验证未登录401、跨项目403和管理员
  完整GIS写入链；
- 隔离集成流使用SQLite GIS Provider；S3 SigV4签名、密钥不泄漏和filesystem对象路径防逃逸
  由Provider单测覆盖；
- SQLite在线备份、SHA-256清单和恢复前校验命令已在临时目录实跑通过；
- 浏览器桌面/移动布局、筛选、深链和无Key降级验收通过；
- Demo完整性校验通过；
- 原smart-renew项目边界校验通过；脚本只忽略既有`.DS_Store`系统元数据，GIS实现未修改
  原业务源码。

### 11.3 唯一外部验收项

真实高德在线验收仍需预生产环境提供`AMAP_JS_KEY`、`AMAP_JS_SECURITY_CODE`和
`AMAP_WEB_SERVICE_KEY`。上线前须按`docs/deployment/gis-production-deployment.md`
验证JS底图、地址质量、真实POI、配额和错误码；在完成前不得声明高德生产账号已验收。

## 12. 2026-08-03 GIS工程化收口记录

### 12.1 本地缺口已收口

- 建立Playwright双服务隔离E2E，覆盖无Key真实SVG降级、筛选/图层/URL刷新恢复、
  375px清单/地图切换和地图读接口失败；
- 建立空数据、少量数据、密集数据、卫星道路降级和错误告警5类PNG视觉基线，
  按1%差异像素比执行门禁；
- 增加100个空间分析历史运行和50个确定性SVG地图快照容量测试，覆盖有界返回和
  25+25分页；
- 地图快照改为持久化后台Runner：HTTP 202返回`queued`，前端轮询，并发范围1—8，
  支持`running`中断任务重启恢复与失败重试；内部冻结payload不返回浏览器；
- Business正式问题接入SQLite Provider，与其他GIS记录一同原子同步RTree空间索引，包含旧JSON数据事务迁移、
  `listInBounds`和查询计划测试；
- 路线清洗对低精度/异常速度缺口生成MultiLineString显示几何，高德层、SVG降级和报告
  地图快照都不再跨异常点连线；
- 新增`MapOverlayLayer`，统一`setData/setVisible/setSelected/clear/destroy`契约，并拆分
  layer-control、selection和snapshot-view前端模块。

### 12.2 验证结果

- `npm run check`通过；
- `npm test`通过，195/195；
- `npm run test:integration`通过，1/1；
- `npm run test:e2e`通过，9/9，包含5类持久视觉基线差异比对；
- `npm run verify:demo`通过，42文件；
- `npm run verify:boundary`和`git diff --check`通过。

### 12.3 当前结论

V9.1 GIS大纲中本地可实现、可自动化验收的开发内容已全部收口。唯一外部开始点是预生产真实高德验收：
配置不入库的三类Key，验证JS SDK域名、卫星/道路层、地址和POI质量、配额/错误码与密钥不泄漏，并归档时间和账号归属。

## 13. 2026-08-09 代码审查整改收口（CR-01—CR-08）

本轮依据 `docs/code-review-remediation-development-outline.md` 执行，已完成本地可验证的八项整改：

- CR-01：标准绑定PATCH与审计GET接入可信身份、项目级RBAC、404/401/403边界和真实审计人；编辑者角色增加 `gis.issue.binding.edit`。
- CR-02：Provider迁移默认不覆盖目标冲突，成功项带迁移标记与哈希；运行与回滚结果分别记录新增、跳过、冲突、失败，重复执行/回滚保持幂等。
- CR-03：迁移逐条持久化检查点、心跳和中断状态；运行租约过期后才允许明确恢复，并保留回滚冲突结果。
- CR-04：CloudBase Repository 支持完整读取、offset/limit和条件查询，避免把分页控制字段放入 `where`；新增250条分页单测。
- CR-05：Business锁定 `@cloudbase/node-sdk@3.18.3` 并更新 lockfile；初始化、Collection和对象存储探针错误分类见 CloudBase 部署文档；生产 `productionVerified` 仍为 `false`。
- CR-06：成果汇总遍历全部可见项目，详情最大200条并返回总量/截断标记，批量并发上限20。
- CR-07：成果中心分别统计必需资料未完成项目和建议项警告项目。
- CR-08：设置接口统一要求认证；普通用户仅获得脱敏能力状态，管理员才可读取环境/Collection/探针诊断，前端保留401/403/disabled状态。

本轮回归证据：

- `npm run check` 通过；
- `npm test` 通过，215/215；
- `npm run test:integration` 通过，1/1，覆盖匿名设置401、项目范围403、标准绑定编辑和审计读取；
- `npm run test:e2e` 通过，11/11，覆盖成果中心与系统设置；
- CloudBase Mock覆盖250条分页、目标冲突、中断恢复、回滚第三方修改和 `productionVerified=false`。

真实CloudBase环境、数据库规则、对象存储权限和恢复演练仍是外部验收项；在完成前不把本地Mock/SQLite结果写成生产已验收。
