# 智更平台 Word 报告模板工作流程

> 创建日期：2026-08-11
> 适用项目：智更网页
> CloudBase 环境：`smart-renew-d2gamusvr1b96ce95`
> 目标：使用统一的 Word 模板，按指标编码从项目数据库取数，生成可继续编辑的 `.docx` 报告。

## 一、核心原则

1. Word 报告生成时直接读取当前项目的正式数据，不读取未经人工复核的 AI 候选问题。
2. 指标数据使用 `PRB` 编码作为稳定绑定键，使用指标库“细分小类（编码名称）”作为人员核对标题。
3. Word 模板中的每一项动态内容都必须有明确的数据来源、字段编码和缺失数据处理规则。
4. 每次生成报告时先保存数据快照，再由该快照生成 Word，防止后续数据变化导致旧报告漂移。
5. 生成后的 Word 内容是普通文字、表格和图片，可在 Microsoft Word 中直接修改。
6. 报告版本只新增，不覆盖旧版本；每个版本保留数据截止时间、模板版本和来源数据编号。
7. 每个项目的报告草稿、人工审核状态、数据快照、Word 文件和历史版本必须独立保存，不得与其他项目共用。
8. 报告生成及预览仅能查询与当前 `projectId` 一致的 `projectDataRecords`、`officialIssues`、`photoRecords` 和 `analysisRecords`，不允许跨项目聚合。
9. 全局 Word 文件只是版式母版；项目首次进入报告页时从母版创建独立副本，之后的编辑不会影响其他项目。

### 项目隔离键

- 母版编号：`TPL-WORD-V1`。
- 项目报告草稿编号：`TPL-WORD-V1-P-{projectId}`。
- 项目指标索引条件：`projectDataRecords.projectId === currentProjectId`。
- 报告版本条件：`reportSnapshots.projectId === currentProjectId`。
- 任何报告 API 都必须显式携带项目编号，不提供无项目范围的混合查询。

## 二、目标使用流程

1. 项目建档并完成住宅台账。
2. 上传现场照片，执行 AI 识别。
3. 人工复核问题，正式写入问题台账。
4. 完成社区／街区分析及其他项目数据录入。
5. 进入“报告成果”，选择 Word 模板版本。
6. 系统按字段字典检查必填项、缺失数据和未关联记录。
7. 生成报告数据快照。
8. 使用快照填充 Word 模板，生成 `.docx` 文件。
9. 将 Word 文件存入 CloudBase 存储，并建立 V1、V2 等版本记录。
10. 用户下载 Word，在本地继续编辑。

## 三、数据绑定方式

### 1. 稳定键与显示名称

- 稳定键：`PRB-03-01`
- 核对标题：`楼梯踏步缺损`
- Word 引用示例：`{{PRB-03-01.issueCount}}`

指标名称修改时，只更新显示标题，不修改已建立的 `PRB` 编码绑定。

### 2. 每个指标小标题的标准字段

| 字段 | 用途 |
| --- | --- |
| `code` | `PRB` 编码 |
| `title` | 指标库细分小类名称 |
| `issueCount` | 正式问题数量 |
| `communityCount` | 涉及小区数量 |
| `buildingCount` | 涉及楼栋数量 |
| `householdCount` | 涉及户数 |
| `highCount` | 高风险数量 |
| `mediumCount` | 中风险数量 |
| `lowCount` | 低风险数量 |
| `locations` | 问题位置集合 |
| `descriptions` | 经复核的问题描述 |
| `suggestions` | 整改建议 |
| `issues` | 问题明细，用于循环表格 |
| `originalPhotos` | 原始现场照片 |
| `annotatedPhotos` | AI 标注图 |
| `sourceIds` | 数据来源编号 |

### 3. 非指标类字段

- 项目档案：名称、位置、类型、范围、面积和说明。
- 住宅台账：小区、楼栋、户数和关联编号。
- 社区／街区分析：分析维度、搜索范围、设施总数、分类数量和结论。
- 报告元数据：版本、生成人、生成时间、数据截止时间和模板版本。

## 四、报告内容类型

1. **固定文字**：所有项目通用的说明。
2. **动态段落**：文字中插入项目字段或指标字段。
3. **条件段落**：按“有问题／无问题”或风险等级选择不同表述。
4. **循环表格**：按问题、小区、楼栋或指标数量自动增加行。
5. **动态图片**：插入原始照片、AI 标注图或地图成果。
6. **来源附录**：记录本版本使用的项目、问题、照片、分析和指标数据编号。

## 五、分步实施计划

### 第 1 步：确定 Word 原始模板

**目标**

确定长期复用的报告版式、章节、封面、目录、表格和页眉页脚。

**用户需要提供**

- 一份可编辑的 `.docx` 原始文件。
- 说明哪些章节必须保留，哪些只是参考。
- 如果只有 PDF，需先转换或重建为 Word，并确认转换后版式。

**产物**

- `report-template-v1.docx`
- 章节清单及模板范围确认。

**验收标准**

- Word 能正常打开和编辑。
- 页面尺寸、字体、标题级别、表格和页码符合正式报告要求。

### 第 2 步：建立报告字段字典

**目标**

将项目数据库字段、指标库 `PRB` 小标题和 Word 可引用字段统一起来。

**开发操作**

- 盘点 `projects`、`projectDataRecords`、`officialIssues`、`photoRecords`、`analysisRecords` 和社区／街区分析数据。
- 以 `PRB` 编码为主键建立聚合规则。
- 建立中文小标题、技术字段、数据类型和缺失值规则。

**用户配合**

- 核对小标题名称。
- 确认每项数据为空时是显示“暂无”、显示 0，还是不生成该段落。

**产物**

- 报告字段字典。
- `PRB` 指标数据聚合规则。

### 第 3 步：逐章标记 Word 引用内容

**目标**

把 Word 模板中的动态位置替换为结构化引用。

**操作方式**

- 按章节逐项处理，不一次性批量猜测。
- 每一段记录：段落原文、引用字段、显示条件、缺失数据处理和数据来源。
- 使用“编码＋小标题”显示绑定结果，便于人工核对。

**用户配合**

- 编辑或确认每条动态段落的最终表述。
- 确认表格列、图片位置和条件段落。

**产物**

- 已标记引用字段的 Word 模板。
- 章节与数据引用对照表。

### 第 4 步：扩充报告数据快照

**目标**

使报告快照完整包含 Word 生成所需的指标、明细、图片和来源。

**开发操作**

- 将 `projectDataRecords` 及其来源编号纳入快照。
- 按 `PRB` 编码聚合问题、小区、楼栋、户数和风险等级。
- 区分原始现场照片和 AI 标注图。
- 增加模板版本、字段字典版本和数据截止时间。

**产物**

- 新版报告快照结构。
- 报告快照 API 更新。

### 第 5 步：开发 Word 生成能力

**目标**

由指定报告快照和指定模板生成可编辑的 `.docx`。

**开发操作**

- 读取 Word 模板。
- 填充文字、数字、条件段落、循环表格和图片。
- 生成 `.docx` 文件并上传至智更 CloudBase 存储。
- 保存 Word 文件编号、模板版本和生成状态。

**产物**

- Word 生成服务。
- Word 下载接口。

### 第 6 步：改造“报告成果”页面

**目标**

用 Word 模板、数据预检和 Word 版本替代当前固定 PDF 原文阅读器。

**页面区域**

- 报告模板：显示模板名称、版本和适用范围。
- 数据预检：按指标小标题显示已有数据、缺失数据和未关联数据。
- 报告预览：预览将要填入 Word 的关键内容。
- Word 版本：生成、下载并查看 V1、V2 等历史版本。

**产物**

- 新的报告成果工作台。

### 第 7 步：用真实项目验收

**目标**

验证同一份模板能为不同项目生成数据正确、可继续编辑的 Word。

**验收项目**

- 西安雁塔区城市体检。
- 绵阳城市体检。

**验收内容**

- 指标小标题、`PRB` 编码和 Word 数据一致。
- 项目数量、问题数量、风险统计、照片和问题明细一致。
- 无数据指标按已确认规则显示。
- Word 目录、页码、表格、图片和中文字体正常。
- 下载后可直接修改文字并另存。

## 六、报告版本建议结构

```text
reportSnapshots
├─ id
├─ projectId
├─ version
├─ templateId
├─ templateVersion
├─ fieldDictionaryVersion
├─ generatedBy
├─ generatedAt
├─ dataCutoffAt
├─ snapshot
│  ├─ project
│  ├─ housing
│  ├─ photos
│  ├─ analyses
│  ├─ issues
│  ├─ indicatorsByCode
│  └─ communityAnalysis
├─ sourceIds
│  ├─ projectIds
│  ├─ projectDataRecordIds
│  ├─ communityIds
│  ├─ buildingIds
│  ├─ photoIds
│  ├─ analysisIds
│  └─ issueIds
├─ wordFileId
├─ wordFileName
└─ status
```

## 七、当前进度

- [x] 确定“Word 模板＋数据库引用＋可编辑下载”的总体方向。
- [x] 确定“`PRB` 编码绑定＋指标小标题核对”的方式。
- [x] 建立本工作流程文档。
- [x] 第 1 步：获取并确认 Word 原始模板。
- [x] 将正式 Word 转换为网页可编辑的有序段落、表格和图片模型。
- [x] 增加模板人工审核状态：待审内容标黄，审核通过后取消黄色。
- [x] 在“报告成果”页增加 Word 正式模板编辑与审核工作台。
- [ ] 第 2 步：建立报告字段字典。
- [ ] 第 3 步：逐章标记 Word 引用内容。
- [ ] 第 4 步：扩充报告数据快照。
- [ ] 第 5 步：开发 Word 生成能力。
- [ ] 第 6 步：改造报告成果页面。
- [ ] 第 7 步：使用真实项目验收。

## 八、第 1 步完成记录

- 正式模板已确认：`assets/report-templates/report-template-v1.docx`。
- 保留现有 Word 版式。
- 保留全部章节。
- 原始 Word 保持不变，项目内使用副本开展后续标记和生成工作。
- 旧项目筛查结果见：`REPORT_TEMPLATE_V1_SCREENING.md`。

## 九、模板网页审核规则

- 从旧项目 Word 导入的非标题正文、图片和表格数据行默认标记为黄色待审内容。
- 每个待审段落和表格行单独审核，不默认批量通过。
- 审核前必须填写审核人员。
- 审核通过后保存审核人和审核时间，并取消黄色。
- 已审核内容再次修改后自动恢复为黄色待审状态。
- 模板草稿保存正文、表格和审核状态；原始 `.docx` 母版保持不变。

## 十、制式内容确认记录

2026-08-11 根据网页批注确认，以下内容作为制式内容，默认不标黄、不要求人工审核：

- 1.1 工作背景的前两段通用说明。
- 1.2 工作目标正文。
- 1.3.1、1.3.2、1.3.3 工作原则正文。
- 1.3 体检评估范围的四维度通用说明。
- 1.4.1 中“指标体系确定”和“数据采集调查”的标题及通用说明。
- 1.4.1 中街区维度、社区（小区）维度的组织方式说明。
- 1.4.1 中“成效问题评估”“强化成果应用”的标题及正文。
- 1.4.2 技术路线正文、技术路线图及图题。

同时恢复 Word 自动编号中未被文本提取的前四个一级章节序号：“一、”至“四、”。
