# 智更项目长期约定

## 视觉模型架构

- 本项目同时保留两套视觉模型，不能互相覆盖或删除，网页端应允许用户选择。
- **千问视觉模型**：走云端服务，由智更 CloudBase API 调用，不依赖姚工电脑或单位局域网。
- **单位视觉模型（集团视觉模型）**：接口只能从单位内网访问，因此必须经过姚工当前电脑上的本机／局域网代理，端口为 `4173`。
- 姚工电脑是单位视觉模型的代理服务器。电脑关机、休眠、断网、离开单位内网或代理进程停止时，单位视觉模型不可用；千问模型不受影响。
- 姚工在代理电脑上用 `file://` 打开网页时，`http://127.0.0.1:4173` 是正确的本机代理地址，不应将其误判为普通线上接口错误。
- 其他局域网电脑不能使用自己的 `127.0.0.1`，应通过姚工电脑的局域网地址访问整个网站（当前示例为 `http://172.16.71.214:4173`；IP 变化时以实际地址为准）。
- 单位视觉模型的 API Key 只能保存在姚工电脑的私有配置或环境变量中，不得提交到 GitHub、网页代码、CloudBase 配置或 Markdown 文档。
- 不要把 `4173` 端口开放到公网，仅允许可信单位局域网访问。

## 部署注意

- 智更网页部署到 `smart-renew-d2gamusvr1b96ce95` 时，云端只能直接使用千问模型；单位视觉模型仍依赖姚工电脑代理。
- 修改模型调用、代理地址或部署配置前，必须先判断目标是“千问云端链路”还是“单位模型本机代理链路”，避免把两条链路混为一体。

## Git 分支约定

- 日常开发和功能修改统一在 `姚博迪修改` 分支进行，不直接在 `main` 上修改。
- 每个确认稳定的阶段版本，再将 `姚博迪修改` 分支同步一份到 `main` 并推送 GitHub。
- 同步前先检查工作区、备份目录、测试缓存和密钥文件；只提交真实项目文件。
- 不使用强制推送、硬重置或覆盖式切换来同步分支；发现远端分叉时先停止并核对。
