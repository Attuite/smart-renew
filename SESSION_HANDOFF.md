# 智更 Smart Renew 项目会话交接文档

> 用途：在新的 Codex 会话开始时优先读取本文件，快速恢复项目背景、协作规则、已完成内容和后续任务。
>
> 最近整理日期：2026-07-22

## 1. 项目定位

智更 Smart Renew 是一个面向城市更新体检的项目制工作平台。平台围绕一个体检项目持续归集数据，核心业务闭环为：

1. 新建体检项目并建立基础档案。
2. 在住区维度上传现场照片，通过视觉大模型识别建筑隐患。
3. 人工确认、修改或驳回识别问题，并保存原图、标注图和问题台账。
4. 在社区/街区维度选择项目、定位项目位置、搜索周边设施并保存分析结论。
5. 将项目档案、AI 分析记录和空间分析结果汇总到项目中枢。
6. 按“制式章节 + 项目真实数据自动填充”的方式生成项目体检报告。
7. 后续持续沉淀整改、复核和运营记录。

项目的核心原则：**项目是数据主线，所有分析结果必须归档到具体项目下。**

## 2. 仓库与协作规则

- GitHub 仓库：`https://github.com/Attuite/smart-renew`
- 本地目录：`G:\智更平台 - 副本`
- 正式分支：`main`
- 日常开发分支：`姚博迪修改`
- 当前开发基线：以 `origin/姚博迪修改` 最新提交为准。

每次修改必须遵守以下流程：

1. 确认当前分支是 `姚博迪修改`。
2. 修改前执行 `git pull --ff-only origin "姚博迪修改"`。
3. 在本地完成修改和验证。
4. 将本次修改提交并推送到 `origin/姚博迪修改`。
5. 不要自行创建其他分支。

部署规则：

- 只有用户明确说“上线”“部署”或“同步线上”时才允许部署。
- 上线时使用 `main` 分支代码，不直接部署 `姚博迪修改`。
- 若目标改动尚未合并到 `main`，先提醒用户，不得擅自合并。
- 部署前切换到 `main` 并拉取最新代码；部署完成后切回 `姚博迪修改`。

## 3. 当前技术结构

项目目前以单页应用为主：

- `index.html`：页面结构、样式和主要前端业务逻辑，当前是大型单文件应用。
- `server.mjs`：本地 Node.js 服务和代理入口。
- `functions/api/index.js`：CloudBase 云函数 API，负责项目、分析记录、用户 Key 和视觉模型代理。
- `functions/api/package.json`：CloudBase API 依赖清单。
- `cloudbaserc.json`：CloudBase 环境和函数配置。
- `DEPLOYMENT.md`：早期部署说明，部分内容仍偏 Render 方案，后续应更新为当前 CloudBase 方案。
- `assets/`：项目视觉资源与报告文件目录。

本地运行地址通常为：

```text
http://127.0.0.1:4173
```

## 4. CloudBase 线上信息

- CloudBase 环境：`smart-renew-d2gamusvr1b96ce95`
- 静态站点：`https://smart-renew-d2gamusvr1b96ce95-1456348363.tcloudbaseapp.com`
- API 地址：`https://smart-renew-d2gamusvr1b96ce95.service.tcloudbase.com`
- 云函数名称：`api`
- Node.js 运行时：`Nodejs20.19`
- 默认视觉模型：`qwen3-vl-plus`

CloudBase 数据库使用的关键集合包括：

- 项目记录集合。
- AI 分析记录集合。
- `apiKeyUsers`：保存不同用户独立加密的 DashScope API Key 信息。

安全要求：

- 不得把 `.env`、明文 API Key、用户密码、`.smart-renew-data/` 提交到 GitHub。
- `node_modules/`、`output/`、`tmp/` 和本地备份文件已加入 `.gitignore`。
- 前端不能长期保存明文 Key；线上模型调用必须通过服务端代理。

## 5. 已完成的核心功能

### 5.1 项目主线

- 已实现新建体检项目。
- 新建项目已增加必选地图框选范围，保存多边形边界、范围面积和中心点。
- 框选后通过高德地图检索并二次过滤边界内住宅小区，保存真实小区台账。
- 楼栋数和住宅户数支持在创建项目时或项目数据库中人工补录；未核实数据保持“待补录”，不得估算。
- 旧项目可从项目数据库补充或重新框选项目范围。
- 已实现项目汇总页面和项目中枢。
- 项目可设为当前项目，住区、社区/街区和报告页面读取当前项目。
- 项目和分析记录已从单浏览器本地存储扩展为服务端持久化，可跨浏览器查看。
- 项目详情按四个体检维度组织：住房维度、小区/社区维度、街区维度、城区/城市维度。

### 5.2 住区维度与视觉模型

确定的完整流程：

1. 选择项目。
2. 上传并压缩照片。
3. 服务端安全调用视觉模型。
4. 返回问题分类、风险等级、置信度和坐标框。
5. 前端绘制标注框并生成标注图。
6. 自动分类并合并重复问题。
7. 人工确认、修改或驳回。
8. 保存原图、标注图和问题台账。
9. 汇总进入项目中枢和体检报告。

已完成内容：

- 分析类型改为必选项，帮助 AI 聚焦识别任务。
- 巡检区域支持常用选项下拉提示，也允许手动输入。
- 上传数量读取 AI 配置页中的“单次最多上传图片数”。
- AI 配置支持多个真实千问视觉模型选项。
- 已接入 CloudBase 服务端视觉模型代理。
- 已增加人工复核与归档流程。
- 已增加用户级 API Key：用户名和密码对应独立 Key，不同用户互不关联。

### 5.3 社区/街区维度

- 页面名称已统一为“社区/街区维度”。
- 页面进入后必须先选择项目，再选择分析维度、定位、搜索并保存结论。
- 已拆分“社区维度”和“街区维度”两个模式。
- 点击统计分类可滚动并展开对应分类列表。
- 分类列表默认折叠，点击后展开。
- POI 已进行空间合并，商业综合体等空间不再拆成大量细碎商户。
- 地图信息窗文字尺寸已固定，不随地图缩放自行变化。
- 分析结论保存到当前项目下，并进入项目中枢和报告。

社区维度当前明确的 12 类指标：

1. 养老服务。
2. 婴幼儿照护服务设施。
3. 幼儿园数量。
4. 小学数量。
5. 停车场数量。
6. 新能源汽车充电设施。
7. 电动自行车充电设施。
8. 公共活动场地。
9. 步行道。
10. 垃圾分类设施。
11. 物业管理小区。
12. 智能化改造。

### 5.4 体检报告

- 报告页面要求先选择项目。
- 已实现“制式章节 + 项目数据自动填充”的项目级报告预览。
- 可读取项目档案、住区维度问题、社区/街区维度统计和行动建议。
- 已嵌入《绵阳市2025年城市体检总报告》，并提供左侧章节目录和页码跳转。
- 当前正式 PDF 导出仍是待完善能力，页面按钮只提示后续接入。

### 5.5 数据看板

- 已明确禁止虚拟数据和虚拟页面。
- 看板只读取真实保存的项目、AI 分析、风险问题、照片和社区/街区分析记录。
- 没有数据时应展示明确的空状态，不得用示例数字填充。

### 5.6 首页 UI

- 首页改为深色、建筑与城市更新相关的沉浸式设计。
- 第一屏保持单屏展示，不要求用户向下滚动。
- 第一屏按钮文案为“前进”，点击进入第二屏核心能力页面。
- 第二屏展示“体检、诊断、更新、运营”四项核心能力。
- 第二屏建筑缩略图按平行轨道缓慢移动。
- 背景轨道已改为双份连续内容，支持无缝循环，不在中途重启或卡顿。
- 已验证 `1920x920` 和 `1145x920` 两种浏览器尺寸。

## 6. 关键产品决策

- 页面整体流程以项目为中心，不允许各分析模块独立产生无归属数据。
- 各分析页面必须先选择项目。
- 项目汇总不是普通案例展示，而是完成或进行中的体检项目台账。
- 住区维度负责现场照片和建筑隐患识别。
- 社区/街区维度负责项目周边设施空间和服务短板分析。
- 体检报告是前面所有真实数据的最终汇总出口。
- 平台不得出现虚构 KPI、虚构图表、虚构项目和假接口状态。
- UI 要专业性与艺术性共存，但业务页面保持清晰、克制和适合重复操作。
- 本地修改与线上部署必须分离，不能因本地完成修改就自动上线。

## 7. 当前待办事项

优先级较高：

- 用真实建筑照片替换首页第二屏当前的 CSS 建筑纹理占位块。
- 继续校准高德 POI 清洗规则，避免有效的幼儿园、停车场、汽车充电和电动车充电设施被过度过滤。
- 新能源汽车充电分类目前可统计充电站空间；单个站点的真实充电桩数量依赖高德返回字段或其他数据源，不能凭空推算。
- 完善街区维度的正式分类体系和独立搜索词库。
- 把项目中枢四个维度进一步做成可独立采集、评价和生成问题清单的模块。
- 完成项目级报告的正式 PDF 模板映射、分页、附件和导出。
- 更新 `DEPLOYMENT.md`，移除已过时的 Render 主方案，改为当前 CloudBase 部署流程。

技术债务：

- `index.html` 已超过 250 KB，HTML、CSS 和 JavaScript 高度集中，后续宜逐步拆分为模块。
- 当前仍是原生单页应用，不是最初设想的 React + Tailwind 架构；重构前需评估数据迁移和部署影响。
- 高德地图、视觉模型和报告模块需要更系统的错误提示、加载状态和回归测试。
- 需要补充团队协作说明，例如分支策略、提交规范、CloudBase 环境初始化和本地运行步骤。

## 8. 参考文件

当前会话曾用于梳理产品和报告结构的外部文件：

- `D:\xwechat_files\wxid_nx491mkybq3l22_6563\msg\file\2026-06\绵阳市2025年城市体检总报告1031（总报告）(2).pdf`
- `D:\xwechat_files\wxid_nx491mkybq3l22_6563\msg\file\2026-07\城市体检工作手册 .pdf`

这些文件不应直接提交到公开仓库，除非确认版权、文件大小和仓库可见性符合要求。

## 9. 常用检查命令

确认分支并拉取：

```powershell
git switch "姚博迪修改"
git pull --ff-only origin "姚博迪修改"
```

检查前端内联脚本语法：

```powershell
@'
const fs = require('fs');
const html = fs.readFileSync('index.html', 'utf8');
const scripts = [...html.matchAll(/<script[^>]*>([\s\S]*?)<\/script>/g)].map(match => match[1]);
const mainScript = scripts.reduce((current, next) => next.length > current.length ? next : current, '');
new Function(mainScript);
console.log('index inline script syntax ok');
'@ | node -
```

检查 CloudBase 云函数语法：

```powershell
node --check functions\api\index.js
```

同步开发分支：

```powershell
git add <本次修改文件>
git commit -m "简明描述本次修改"
git push origin "姚博迪修改"
```

## 10. 下一次会话启动提示

建议在下一次会话开头直接说明：

```text
请先读取 SESSION_HANDOFF.md，确认当前位于“姚博迪修改”分支并拉取最新代码，再开始本次修改。除非我明确要求上线，否则不要部署；上线只使用 main 分支代码。
```
